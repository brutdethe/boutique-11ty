---
layout: default.njk
---

<img src="{{ settings.dirs.img_products | url }}{{ photos[0] }}" alt="{{ photos[0] }}" />
<br>
<a href="{{ settings.dirs.img_products | url }}{{ photos[1] }}">{{ photos[1] }}</a>

<hr>

<h2>{{ description }}</h2> 

{% for key, value in options %}
    <p>{{ key }} : {{ value }}</p>
{% endfor %}
